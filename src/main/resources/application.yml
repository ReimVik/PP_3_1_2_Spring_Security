spring:
  datasource:
    url: jdbc:mysql://localhost:3306/MyDB
    username: root
    password: Rfgbnjirf1488
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  mvc:
    hiddenmethod:
      filter:
        enabled: true